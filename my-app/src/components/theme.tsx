import { extendTheme } from "@chakra-ui/react"

const theme = extendTheme({
    fonts: {
        heading: `'DM Sans', 'Work Sans'`,
        body: `'DM Sans', 'Work Sans'`
    }
});

export default theme;